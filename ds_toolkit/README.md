# Data Science Toolkit

## Purpose
To have a scaffolder that generates the structure of a typical data science project in a reproducable manner. This eases the process of starting new Data Science projects by automatically installing all of the relevant data science architecture.

## Pre-requisites
Ensure both Python and Git are installed.

### Usage

```bash
python -m ds_toolkit.cli init my_project --venv --git --notebook
```